{% extends '@Backoffice/base.html.twig' %}
{% block content %}
    <link href="//maxcdn.bootstrapcdn.com/bootstrap/3.3.0/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.0/js/bootstrap.min.js"></script>
    <script src="//code.jquery.com/jquery-1.11.1.min.js"></script>
    <!------ Include the above in your HEAD tag ---------->

    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
    <script src="http://getbootstrap.com/dist/js/bootstrap.min.js"></script>
    <h1>Factures list</h1>
    <link href="{{ asset('css/bootstrap2.min.css') }}" rel="stylesheet">
    <!--external css-->
    <link href="{{ asset('css/font-awesome.css')}}" rel="stylesheet" />
    <!-- Custom styles for this template -->
    <link href="{{ asset('css/style.css')}}" rel="stylesheet">
    <link href="{{ asset('css/style-responsive.css')}}" rel="stylesheet">
    <!-- blueimp Gallery styles -->
    <link rel="stylesheet" href="http://blueimp.github.io/Gallery/css/blueimp-gallery.min.css">
    <!-- CSS to style the file input field as button and adjust the Bootstrap progress bars -->
    <link rel="stylesheet" href="{{ asset('css/jquery.fileupload.css')}}">
    <link rel="stylesheet" href="{{ asset('css/jquery.fileupload-ui.css')}}">
    <table>
        <thead>
            <tr>
                <th>Idfacture</th>
                <th>Employeeid</th>
                <th>Fournisseurid</th>
                <th>Typeproduct</th>
                <th>Montant</th>
                <th>Actions</th>


            </tr>
        </thead>
        <tbody>
        {% for facture in factures %}
            <tr>
                <td><a href="{{ path('facture_show', { 'idFacture': facture.idFacture }) }}">{{ facture.idFacture }}</a></td>
                <td>{{ facture.employeeId }}</td>
                <td>{{ facture.fournisseurId }}</td>
                <td>{{ facture.typeProduct }}</td>
                <td>{{ facture.montant }}</td>


                <td>
                    <ul>
                        <li>
                <td><p data-placement="top" data-toggle="tooltip" title="Delete">
                        <a href="{{ path('delete_facture', { 'idFacture': facture.idFacture }) }}">

                        <button class="btn btn-danger btn-xs" data-title="Delete" data-toggle="modal" data-target="#delete" >
                            <span class="glyphicon glyphicon-trash"></span></button></a></p></td>
                {#<a href="{{ path('delete_facture', { 'idFacture': facture.idFacture }) }}">show</a>#}

                </li>




                <td><p data-placement="top" data-toggle="tooltip" title="Edit">
                        <a href="{{ path('facture_edit', { 'idFacture': facture.idFacture }) }}">
                            <button class="btn btn-primary btn-xs" data-title="Edit" data-toggle="modal" data-target="#edit" >
                                <span class="glyphicon glyphicon-pencil"></span></button></a></p></td>
                       {# <li>
                            <a href="{{ path('facture_edit', { 'idFacture': facture.idFacture }) }}">edit</a>
                        </li>#}
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <button type="button" class="btn btn-outline-primary"><a href="{{ path('facture_new') }}">Create a new facture</a></button

        </li>
    </ul>
{% endblock %}
